<% if @book.errors.any? %>
    <div class = "alert alert-warning">
        <ul>
        	<% @book.errors.count%>errors
            <% @book.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
        </ul>
    </div>
<% end %>